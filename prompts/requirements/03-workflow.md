# Workflow

1. The browser creates or recovers a local decryption key.
2. The user selects a file.
3. The browser encrypts the file before transmission.
4. The server stores the encrypted payload and metadata.
5. The browser later retrieves and decrypts the file locally.
6. Users can share decryption keys explicitly with other users.
