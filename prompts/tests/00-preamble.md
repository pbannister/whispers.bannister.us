# Tests Overview

The tests directory should contain a small set of clear, focused test documents for the website.

- Tests should be stored as separate files with descriptive names.
- The test set should include checks that are performed before deployment and checks that are performed after deployment.
- The tests should cover core functionality, privacy expectations, and the collection-based workflow.

